# Starship 提示符配置
# 放置在 ~/.config/starship.toml
# 简洁美观的单行提示符

# 整体配置 - 双行格式（信息 + 光标分离）
format = """
$username\
$hostname\
$directory\
$git_branch\
$git_status\
$package\
$c\
$python\
$nodejs\
$rust\
$golang\
$bun\
$java\
$lua\
$php\
$ruby\
$docker_context\
$kubernetes\
$aws\
$gcloud\
$cmd_duration\
$jobs
$character"""

# 添加换行
add_newline = true

# ===== 用户名 =====
[username]
show_always = false
format = "[$user]($style)@"
style_user = "bold #89b4fa"  # Catppuccin 蓝色
style_root = "bold #f38ba8"  # Catppuccin 红色

# ===== 主机名 =====
[hostname]
ssh_only = false
format = "[$hostname]($style) "
style = "bold #89b4fa"
disabled = false

# ===== 目录 =====
[directory]
format = "in [$path]($style)[$read_only]($read_only_style) "
style = "bold #89dceb"  # Catppuccin 青色
truncation_length = 3
truncate_to_repo = true
truncation_symbol = "…/"
read_only = " "
home_symbol = " ~"

# ===== Git 分支 =====
[git_branch]
format = "on [$symbol$branch]($style) "
symbol = " "
style = "bold #cba6f7"  # Catppuccin 紫色

# ===== Git 状态 =====
[git_status]
format = '([$all_status$ahead_behind]($style) )'
style = "bold #f38ba8"  # Catppuccin 红色
conflicted = "="
ahead = "⇡${count}"
behind = "⇣${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"
untracked = "?${count}"
stashed = "$"
modified = "!${count}"
staged = "+${count}"
renamed = "»${count}"
deleted = "✘${count}"

# ===== 命令执行时间 =====
[cmd_duration]
min_time = 500
format = "took [$duration]($style) "
style = "bold #f9e2af"  # Catppuccin 黄色
show_milliseconds = false

# ===== 字符提示符 =====
[character]
success_symbol = "[❯](bold #a6e3a1)"  # Catppuccin 绿色
error_symbol = "[❯](bold #f38ba8)"    # Catppuccin 红色
vicmd_symbol = "[❮](bold #a6e3a1)"

# ===== Python =====
[python]
format = 'via [$symbol$version]($style)[\($virtualenv\)]($style) '
symbol = " "
style = "bold #f9e2af"  # Catppuccin 黄色
pyenv_version_name = false
detect_extensions = ["py"]
detect_files = [".python-version", "Pipfile", "pyproject.toml", "requirements.txt", "setup.py", "tox.ini"]

# ===== Node.js =====
[nodejs]
format = "via [$symbol$version]($style) "
symbol = " "
style = "bold #a6e3a1"  # Catppuccin 绿色
detect_files = ["package.json", ".node-version", ".nvmrc"]

# ===== Rust =====
[rust]
format = "via [$symbol$version]($style) "
symbol = " "
style = "bold #fab387"  # Catppuccin 橙色
detect_files = ["Cargo.toml", "Cargo.lock"]

# ===== Go =====
[golang]
format = "via [$symbol$version]($style) "
symbol = " "
style = "bold #89dceb"  # Catppuccin 天蓝色
detect_files = ["go.mod", "go.sum", "go.work"]

# ===== Bun =====
[bun]
format = "via [$symbol$version]($style) "
symbol = "🥟 "
style = "bold #fab387"  # Catppuccin 橙色
detect_files = ["bun.lockb", "bunfig.toml"]

# ===== Docker =====
[docker_context]
format = "via [$symbol$context]($style) "
symbol = " "
style = "bold #89b4fa"  # Catppuccin 蓝色
only_with_files = true
detect_files = ["docker-compose.yml", "docker-compose.yaml", "Dockerfile"]

# ===== 包版本 =====
[package]
format = "is [$symbol$version]($style) "
symbol = "󰏗 "
style = "bold #f5c2e7"  # Catppuccin 粉色
disabled = false

# ===== Kubernetes =====
[kubernetes]
format = 'on [󱃾 $context \($namespace\)]($style) '
style = "bold #89b4fa"  # Catppuccin 蓝色
disabled = false
detect_files = ["k8s", "kubernetes"]
detect_folders = ["k8s", "kubernetes"]

# ===== AWS =====
[aws]
format = 'on [$symbol($profile )(\($region\) )]($style)'
symbol = "  "
style = "bold #fab387"  # Catppuccin 橙色
disabled = false

# ===== Google Cloud =====
[gcloud]
format = 'on [$symbol$account(@$domain)(\($region\))]($style) '
symbol = "󱇶 "
style = "bold #89b4fa"  # Catppuccin 蓝色
disabled = false

# ===== 时间 =====
[time]
disabled = true
format = 'at [ $time]($style) '
time_format = "%R"
style = "bold #cdd6f4"  # Catppuccin 白色

# ===== 电池 =====
[battery]
full_symbol = " "
charging_symbol = " "
discharging_symbol = " "
disabled = true

# ===== 内存使用 =====
[memory_usage]
disabled = true
threshold = -1
symbol = "󰍛 "
style = "bold dimmed #cdd6f4"

# ===== 任务数 =====
[jobs]
symbol = " "
style = "bold #89b4fa"
number_threshold = 1
symbol_threshold = 1

# ===== 换行 =====
[line_break]
disabled = false

# ===== C/C++ =====
[c]
format = "via [$symbol$version]($style) "
symbol = " "
style = "bold #89b4fa"
detect_extensions = ["c", "h"]

# ===== Java =====
[java]
format = "via [$symbol$version]($style) "
symbol = " "
style = "bold #f38ba8"
detect_extensions = ["java", "class", "jar"]

# ===== Lua =====
[lua]
format = "via [$symbol$version]($style) "
symbol = " "
style = "bold #89b4fa"
detect_extensions = ["lua"]

# ===== PHP =====
[php]
format = "via [$symbol$version]($style) "
symbol = " "
style = "bold #cba6f7"
detect_extensions = ["php"]

# ===== Ruby =====
[ruby]
format = "via [$symbol$version]($style) "
symbol = " "
style = "bold #f38ba8"
detect_extensions = ["rb"]

